server:
  port: 8763
spring:
  application:
    name: service-hi
  zipkin:
    base-url: http://192.168.1.100:9411
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"

